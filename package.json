{
  "name": "@react-boost/monorepo",
  "private": true,
  "workspaces": [
    "packages/bootstrap",
    "packages/common",
    "packages/material",
    "packages/tauri-v1"
  ],
  "dependencies": {},
  "devDependencies": {
    "eslint": "^9",
    "lerna": "^8.1.9",
    "typescript": "^5"
  },
  "scripts": {
    "build": "NODE_OPTIONS=--max-old-space-size=8192 npx lerna run build --no-progress"
  }
}
